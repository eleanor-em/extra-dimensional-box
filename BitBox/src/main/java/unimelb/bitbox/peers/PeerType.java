package unimelb.bitbox.peers;

/**
 * Enumeration to store whether we connected to the peer (outgoing), or the peer connected to us (incoming).
 *
 * @author Eleanor McMurtry
 */
public enum PeerType {
    OUTGOING,
    INCOMING
}
